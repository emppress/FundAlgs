#include <iostream>
#include <cmath>
#include <vector>
#include <bitset>

class encode
{
private:
    std::vector<std::byte> S, key;

public:
    encode(std::vector<std::byte> &key) : S(256), key(key){};
    
};


int main()
{
    
    return 0;
}